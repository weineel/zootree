# `agent_cli` 配置与 `zootree start --run-agent` 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `GlobalConfig` 中新增 `agent_cli` 字段并为 `zootree start` 增加 `--run-agent` flag，启动后在 zellij 默认布局的指定 pane 中以 workspace `title + description` 为 prompt 执行配置的 agent 命令。

**Architecture:** 新增的 `build_prompt` 与 `build_agent_cli_kdl` 是不依赖 I/O 的纯函数，前者拼装 prompt，后者把 `agent_cli` 模板用 `shlex` 拆成 argv 并渲染为 KDL 片段（`command="..." { args "..." }`）。`LayoutVar` 增加 `overview_agent_cli` / `repo_agent_cli` 两个字段；`launch_zellij` 根据 `--run-agent` 与 repo 数量按规则表把 KDL 片段塞到对应字段中，由 `LayoutRenderer::replace_vars` 完成最终替换。

**Tech Stack:** Rust 2021、`shlex = "1.3"`、`anyhow`、`clap` derive、现有 KDL 模板渲染。

**规则表（核心行为）：**

| `--run-agent` | repo 数量 | overview 最后 pane | 每个 repo tab 的 70% pane |
|---|---|---|---|
| false | 任意 | 空（普通 shell） | 空（普通 shell） |
| true | 1 | 空 | 跑 agent |
| true | ≥2 | 跑 agent | 空 |

---

## File Structure

| 文件 | 改动类型 | 责任 |
|------|---------|------|
| `Cargo.toml` | 修改 | 新增 `shlex = "1.3"` 直接依赖 |
| `src/config/global.rs` | 修改 | `GlobalConfig` 增加 `pub agent_cli: Option<String>` |
| `src/core/layout.rs` | 修改 | `LayoutVar` 增加两字段；新增 `kdl_escape`、`build_agent_cli_kdl`、`build_prompt`；`replace_vars` 增加两条替换；`default_layout()` 注入两个占位符 |
| `src/cli/workspace.rs` | 修改 | `StartArgs` 增加 `pub run_agent: bool`；`launch_zellij` 按规则表构造 `LayoutVar` 并对未命中占位符做 `tracing::warn!` |
| `tests/layout_test.rs` | 修改 | 已有 `LayoutVar` 字面量补两个新字段；新增两条 agent_cli 渲染测试 |
| `tests/agent_cli_test.rs` | 新建 | `build_prompt` + `build_agent_cli_kdl` + `kdl_escape` 单测 |
| `tests/start_agent_test.rs` | 新建 | 端到端：用 `LayoutRenderer::render(default_layout(), vars)` 验证规则表行为 |
| `.claude/skills/zootree-dev/SKILL.md` | 修改 | 在「关键依赖」追加 `shlex`，「常见开发任务」追加 agent_cli 提示（见 Task 8） |

---

## Task 1: 新增 `shlex` 依赖

**Files:**
- Modify: `Cargo.toml`

- [ ] **Step 1: 在 `[dependencies]` 区块末尾追加 `shlex = "1.3"`**

```toml
shlex = "1.3"
```

完整 `[dependencies]` 区块示例：

```toml
[dependencies]
clap = { version = "4", features = ["derive"] }
dialoguer = "0.11"
toml = "0.8"
serde = { version = "1", features = ["derive"] }
kdl = "6"
dirs = "6"
glob = "0.3"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
tracing-appender = "0.2"
chrono = { version = "0.4", features = ["serde"] }
rand = "0.8"
anyhow = "1"
shellexpand = "3"
clap_complete = { version = "4.5", features = ["unstable-dynamic"] }
shlex = "1.3"
```

- [ ] **Step 2: 验证依赖能解析**

Run: `cargo check`
Expected: 编译通过；`Cargo.lock` 中 `shlex` 已经存在（1.3.0），不会触发 registry 拉取。

- [ ] **Step 3: Commit**

```bash
git add Cargo.toml Cargo.lock
git commit -m "chore: add shlex 1.3 dependency for agent_cli parsing"
```

---

## Task 2: `GlobalConfig` 增加 `agent_cli` 字段

**Files:**
- Modify: `src/config/global.rs`
- Test: `tests/config_test.rs`

- [ ] **Step 1: 在 `tests/config_test.rs::test_parse_global_config_full` 中加入 `agent_cli` 断言**

把 `test_parse_global_config_full` 函数完整替换为：

```rust
#[test]
fn test_parse_global_config_full() {
    let toml_str = r#"
workspace_root = "~/zootree-workspaces"
branch_prefix = "zootree"
copy_files = [".env"]
agent_cli = "claude --dangerously-skip-permissions -- $prompt"

[zellij]
layout = "default"

[hooks]
post_create = "echo hello"

[log]
max_files = 5
"#;
    let config: GlobalConfig = toml::from_str(toml_str).unwrap();
    assert_eq!(config.zellij.layout, Some("default".into()));
    assert_eq!(config.workspace_root, "~/zootree-workspaces");
    assert_eq!(config.branch_prefix, "zootree");
    assert_eq!(config.copy_files, vec![".env"]);
    assert_eq!(
        config.hooks.post_create,
        Some(zootree::config::global::HookValue::Simple(
            "echo hello".into()
        ))
    );
    assert_eq!(config.log.max_files, Some(5));
    assert_eq!(
        config.agent_cli.as_deref(),
        Some("claude --dangerously-skip-permissions -- $prompt")
    );
}
```

并在 `test_parse_global_config_defaults` 末尾加一行：

```rust
    assert!(config.agent_cli.is_none());
```

- [ ] **Step 2: 运行测试验证失败（字段尚未添加）**

Run: `cargo test --test config_test test_parse_global_config_full -- --exact`
Expected: 编译错误，`no field 'agent_cli' on type 'GlobalConfig'`。

- [ ] **Step 3: 在 `src/config/global.rs::GlobalConfig` 中添加字段**

在 `pub struct GlobalConfig { ... }` 末尾追加：

```rust
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub agent_cli: Option<String>,
```

完整 struct 应为：

```rust
#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
pub struct GlobalConfig {
    #[serde(default)]
    pub zellij: ZellijConfig,
    #[serde(default = "default_workspace_root")]
    pub workspace_root: String,
    #[serde(default = "default_branch_prefix")]
    pub branch_prefix: String,
    #[serde(default)]
    pub copy_files: Vec<String>,
    #[serde(default)]
    pub hooks: HooksConfig,
    #[serde(default)]
    pub log: LogConfig,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub agent_cli: Option<String>,
}
```

并在 `Default for GlobalConfig` 中添加 `agent_cli: None,`：

```rust
impl Default for GlobalConfig {
    fn default() -> Self {
        Self {
            zellij: ZellijConfig::default(),
            workspace_root: default_workspace_root(),
            branch_prefix: default_branch_prefix(),
            copy_files: Vec::new(),
            hooks: HooksConfig::default(),
            log: LogConfig::default(),
            agent_cli: None,
        }
    }
}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `cargo test --test config_test`
Expected: 全部测试通过。

- [ ] **Step 5: Commit**

```bash
git add src/config/global.rs tests/config_test.rs
git commit -m "feat(config): add optional agent_cli field to GlobalConfig"
```

---

## Task 3: `build_prompt` 纯函数

**Files:**
- Modify: `src/core/layout.rs`
- Test: `tests/agent_cli_test.rs`（新建）

- [ ] **Step 1: 创建 `tests/agent_cli_test.rs` 并写入 `build_prompt` 测试**

```rust
use zootree::config::global::ZellijConfig;
use zootree::config::workspace::WorkspaceConfig;
use zootree::core::layout::build_prompt;

fn make_workspace(title: &str, description: &str) -> WorkspaceConfig {
    WorkspaceConfig {
        title: title.into(),
        name: "test-ws".into(),
        description: description.into(),
        branch: "zootree/test".into(),
        workspace_dir: "/tmp/ws".into(),
        created_at: "2026-05-12T00:00:00+08:00".into(),
        zellij: ZellijConfig::default(),
        repos: Vec::new(),
        events: Vec::new(),
    }
}

#[test]
fn build_prompt_uses_title_only_when_description_empty() {
    let ws = make_workspace("Add login flow", "");
    assert_eq!(build_prompt(&ws), "Add login flow");
}

#[test]
fn build_prompt_joins_title_and_description_with_newline() {
    let ws = make_workspace("Add login", "Implement OAuth2");
    assert_eq!(build_prompt(&ws), "Add login\nImplement OAuth2");
}
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cargo test --test agent_cli_test`
Expected: 编译错误，`build_prompt` 不存在。

- [ ] **Step 3: 在 `src/core/layout.rs` 顶部加 `use` 并实现 `build_prompt`**

在文件最顶部添加：

```rust
use crate::config::workspace::WorkspaceConfig;
```

在文件末尾（`impl LayoutRenderer` 之后）添加：

```rust
pub fn build_prompt(workspace: &WorkspaceConfig) -> String {
    if workspace.description.is_empty() {
        workspace.title.clone()
    } else {
        format!("{}\n{}", workspace.title, workspace.description)
    }
}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `cargo test --test agent_cli_test`
Expected: 两个测试通过。

- [ ] **Step 5: Commit**

```bash
git add src/core/layout.rs tests/agent_cli_test.rs
git commit -m "feat(layout): add build_prompt helper for agent_cli prompt construction"
```

---

## Task 4: `kdl_escape` + `build_agent_cli_kdl` 纯函数

**Files:**
- Modify: `src/core/layout.rs`
- Test: `tests/agent_cli_test.rs`

- [ ] **Step 1: 在 `tests/agent_cli_test.rs` 末尾追加 `build_agent_cli_kdl` 测试**

```rust
use zootree::core::layout::build_agent_cli_kdl;

#[test]
fn build_agent_cli_kdl_basic_with_prompt() {
    let kdl = build_agent_cli_kdl("claude -- $prompt", "hello").unwrap();
    assert_eq!(kdl, r#"command="claude" {
    args "--" "hello"
}"#);
}

#[test]
fn build_agent_cli_kdl_no_args_block_when_only_command() {
    let kdl = build_agent_cli_kdl("claude", "ignored").unwrap();
    assert_eq!(kdl, r#"command="claude""#);
}

#[test]
fn build_agent_cli_kdl_ignores_prompt_when_no_placeholder() {
    let kdl = build_agent_cli_kdl("gemini chat", "ignored").unwrap();
    assert_eq!(kdl, r#"command="gemini" {
    args "chat"
}"#);
}

#[test]
fn build_agent_cli_kdl_embedded_prompt_token() {
    let kdl = build_agent_cli_kdl("claude --prompt=$prompt", "hello world").unwrap();
    assert_eq!(kdl, r#"command="claude" {
    args "--prompt=hello world"
}"#);
}

#[test]
fn build_agent_cli_kdl_escapes_double_quote_in_prompt() {
    let kdl = build_agent_cli_kdl("claude -- $prompt", r#"say "hi""#).unwrap();
    assert!(kdl.contains(r#""--" "say \"hi\"""#), "got: {}", kdl);
}

#[test]
fn build_agent_cli_kdl_escapes_backslash_in_prompt() {
    let kdl = build_agent_cli_kdl("claude -- $prompt", r"path\to\file").unwrap();
    assert!(kdl.contains(r#""--" "path\\to\\file""#), "got: {}", kdl);
}

#[test]
fn build_agent_cli_kdl_escapes_newline_in_prompt() {
    let kdl = build_agent_cli_kdl("claude -- $prompt", "line1\nline2").unwrap();
    assert!(kdl.contains(r#""--" "line1\nline2""#), "got: {}", kdl);
}

#[test]
fn build_agent_cli_kdl_empty_template_errors() {
    let err = build_agent_cli_kdl("", "any").unwrap_err();
    let msg = format!("{:#}", err);
    assert!(msg.contains("agent_cli is empty"), "got: {}", msg);
}

#[test]
fn build_agent_cli_kdl_unclosed_quote_errors() {
    let err = build_agent_cli_kdl(r#"claude "unclosed"#, "any").unwrap_err();
    let msg = format!("{:#}", err);
    assert!(
        msg.contains("failed to parse agent_cli"),
        "got: {}",
        msg
    );
}
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cargo test --test agent_cli_test`
Expected: 编译错误，`build_agent_cli_kdl` 不存在。

- [ ] **Step 3: 在 `src/core/layout.rs` 实现两个函数**

在 `build_prompt` 之后追加：

```rust
fn kdl_escape(s: &str) -> String {
    s.replace('\\', r"\\")
        .replace('"', r#"\""#)
        .replace('\n', r"\n")
        .replace('\r', r"\r")
        .replace('\t', r"\t")
}

pub fn build_agent_cli_kdl(agent_cli_tpl: &str, prompt: &str) -> anyhow::Result<String> {
    let tokens = shlex::split(agent_cli_tpl)
        .ok_or_else(|| anyhow::anyhow!("failed to parse agent_cli: {}", agent_cli_tpl))?;
    if tokens.is_empty() {
        anyhow::bail!("agent_cli is empty");
    }

    let substituted: Vec<String> = tokens
        .into_iter()
        .map(|t| t.replace("$prompt", prompt))
        .collect();

    let command = kdl_escape(&substituted[0]);
    let args = &substituted[1..];

    if args.is_empty() {
        Ok(format!(r#"command="{}""#, command))
    } else {
        let escaped_args: Vec<String> = args
            .iter()
            .map(|a| format!(r#""{}""#, kdl_escape(a)))
            .collect();
        Ok(format!(
            "command=\"{}\" {{\n    args {}\n}}",
            command,
            escaped_args.join(" ")
        ))
    }
}
```

注意点：
- `kdl_escape` 顺序很重要——必须先转义反斜杠，再转义其它字符，否则后面的 `\` 会被反复展开。
- 输出格式与测试的精确字符串一致：`command="claude" {\n    args "--" "hello"\n}`（4 空格缩进，没有外层换行）。

- [ ] **Step 4: 运行测试验证通过**

Run: `cargo test --test agent_cli_test`
Expected: 全部 9 个测试通过。

- [ ] **Step 5: Commit**

```bash
git add src/core/layout.rs tests/agent_cli_test.rs
git commit -m "feat(layout): add build_agent_cli_kdl with shlex parsing and KDL escaping"
```

---

## Task 5: `LayoutVar` 增加两字段并扩展 `replace_vars`

**Files:**
- Modify: `src/core/layout.rs`
- Test: `tests/layout_test.rs`

- [ ] **Step 1: 更新 `tests/layout_test.rs` 中所有 `LayoutVar` 字面量补两个新字段**

把每一处 `LayoutVar { ... lazygit_config: ... }` 末尾追加：

```rust
            overview_agent_cli: "".into(),
            repo_agent_cli: "".into(),
```

总共 4 处需要改：
- `test_variable_replacement` 中的 `vars`
- `test_empty_lazygit_config_removes_ucf_arg` 中的 `vars`
- `test_nonempty_lazygit_config_keeps_ucf_arg` 中的 `vars`
- `test_repeat_per_repo` 中的两个 `LayoutVar`（vec 中）

- [ ] **Step 2: 在 `tests/layout_test.rs` 末尾追加两条新测试**

```rust
#[test]
fn test_overview_agent_cli_placeholder_substituted() {
    let template = r#"pane cwd="$workspace_dir" $overview_agent_cli"#;
    let vars = LayoutVar {
        repo_name: "frontend".into(),
        worktree_path: "/ws/frontend".into(),
        branch: "zootree/test".into(),
        workspace_name: "test".into(),
        workspace_dir: "/ws".into(),
        lazygit_config: "".into(),
        overview_agent_cli: r#"command="claude" {
    args "--" "hello"
}"#
        .into(),
        repo_agent_cli: "".into(),
    };
    let result = LayoutRenderer::replace_vars(template, &vars);
    assert!(
        result.contains(r#"cwd="/ws""#),
        "workspace_dir replaced: {}",
        result
    );
    assert!(
        result.contains(r#"command="claude""#),
        "agent_cli kdl injected: {}",
        result
    );
    assert!(
        result.contains(r#"args "--" "hello""#),
        "agent args present: {}",
        result
    );
    assert!(!result.contains("$overview_agent_cli"));
}

#[test]
fn test_empty_agent_cli_leaves_layout_intact() {
    let template = r#"pane cwd="$worktree_path" $repo_agent_cli"#;
    let vars = LayoutVar {
        repo_name: "frontend".into(),
        worktree_path: "/ws/frontend".into(),
        branch: "zootree/test".into(),
        workspace_name: "test".into(),
        workspace_dir: "/ws".into(),
        lazygit_config: "".into(),
        overview_agent_cli: "".into(),
        repo_agent_cli: "".into(),
    };
    let result = LayoutRenderer::replace_vars(template, &vars);
    assert!(!result.contains("$repo_agent_cli"));
    assert!(!result.contains("command=\"claude\""));
    assert!(result.contains(r#"cwd="/ws/frontend""#));
}
```

- [ ] **Step 3: 运行测试验证失败**

Run: `cargo test --test layout_test`
Expected: 编译错误，`LayoutVar` 没有 `overview_agent_cli` / `repo_agent_cli` 字段。

- [ ] **Step 4: 在 `src/core/layout.rs::LayoutVar` 添加两字段**

把 struct 改为：

```rust
pub struct LayoutVar {
    pub repo_name: String,
    pub worktree_path: String,
    pub branch: String,
    pub workspace_name: String,
    pub workspace_dir: String,
    pub lazygit_config: String,
    pub overview_agent_cli: String,
    pub repo_agent_cli: String,
}
```

- [ ] **Step 5: 在 `replace_vars` 末尾追加两行替换**

把 `replace_vars` 改为（注意：`agent_cli` 替换必须放在所有标准变量替换之后，否则 agent_cli 内容里若含 `$workspace_name` 等字面量会被二次替换）：

```rust
pub fn replace_vars(template: &str, vars: &LayoutVar) -> String {
    let mut result = template.to_string();

    // Handle empty lazygit_config: remove "-ucf" "$lazygit_config" pair
    if vars.lazygit_config.is_empty() {
        result = result.replace(r#" "-ucf" "$lazygit_config""#, "");
    }

    result = result.replace("$repo_name", &vars.repo_name);
    result = result.replace("$worktree_path", &vars.worktree_path);
    result = result.replace("$branch", &vars.branch);
    result = result.replace("$workspace_name", &vars.workspace_name);
    result = result.replace("$workspace_dir", &vars.workspace_dir);
    result = result.replace("$lazygit_config", &vars.lazygit_config);
    // agent_cli placeholders MUST be substituted last so injected KDL fragments
    // don't get re-processed by the standard variable replacements.
    result = result.replace("$overview_agent_cli", &vars.overview_agent_cli);
    result = result.replace("$repo_agent_cli", &vars.repo_agent_cli);
    result
}
```

- [ ] **Step 6: 更新 `default_layout()` 注入两个占位符**

把 `default_layout` 函数的返回字符串改为：

```rust
pub fn default_layout() -> &'static str {
    r#"// 自动生成，修改无效，仅作参考和调试用途
layout {
    tab name="overview" {
        pane size=1 borderless=true {
            plugin location="tab-bar"
        }
        pane split_direction="vertical" {
            pane command="zootree" {
                args "list" "--status" "in_progress"
            }
            pane cwd="$workspace_dir" $overview_agent_cli
        }
        pane size=1 borderless=true {
            plugin location="status-bar"
        }
    }

    // @repeat-per-repo
    tab name="$repo_name" {
        pane size=1 borderless=true {
            plugin location="tab-bar"
        }
        pane split_direction="vertical" {
            pane size="60%" command="lazygit" {
                args "-p" "$worktree_path" "-ucf" "$lazygit_config"
            }
            pane {
                pane size="30%" cwd="$worktree_path"
                pane size="70%" cwd="$worktree_path" $repo_agent_cli
            }
        }
        pane size=1 borderless=true {
            plugin location="status-bar"
        }
    }
}"#
}
```

注意：占位符前是一个空格（`cwd="$workspace_dir" $overview_agent_cli`）。当占位符被替换为空串时，结果是 `cwd="$workspace_dir" `（尾部一个空格），KDL 仍合法；当替换为 `command="..." { args "..." }` 时，结果是合法的"pane 同时具有 command 属性和 args 子节点"的形式（与 lazygit pane 等价）。

- [ ] **Step 7: 运行所有测试**

Run: `cargo test`
Expected: 全部测试通过（包括 `tests/layout_test.rs` 的旧测试 + 新增 2 条 + `tests/agent_cli_test.rs` 全部）。

如果 `src/cli/workspace.rs::launch_zellij` 中构造 `LayoutVar` 的代码因新字段缺失而编译失败，**先把 6 行字面量初始化填上空字符串占位**，下个 Task 再做规则表逻辑：

```rust
        vars.push(LayoutVar {
            repo_name: repo_entry.name.clone(),
            worktree_path: format!("{}/{}", ws_dir, repo_entry.name),
            branch: workspace.branch.clone(),
            workspace_name: workspace.name.clone(),
            workspace_dir: ws_dir.clone(),
            lazygit_config,
            overview_agent_cli: String::new(),
            repo_agent_cli: String::new(),
        });
```

- [ ] **Step 8: Commit**

```bash
git add src/core/layout.rs src/cli/workspace.rs tests/layout_test.rs
git commit -m "feat(layout): add overview/repo_agent_cli vars and default-layout placeholders"
```

---

## Task 6: `--run-agent` flag + `launch_zellij` 规则表

**Files:**
- Modify: `src/cli/workspace.rs`

- [ ] **Step 1: 给 `StartArgs` 增加 `run_agent` 字段**

把 `StartArgs` 改为：

```rust
#[derive(Args)]
pub struct StartArgs {
    #[arg(
        help = "Workspace name to start (interactive if omitted)",
        add = ArgValueCompleter::new(|c: &std::ffi::OsStr| complete_workspace(c, WorkspaceFilter::Pending))
    )]
    pub name: Option<String>,
    #[arg(long, help = "Skip launching zellij session after start")]
    pub no_zellij: bool,
    #[arg(long, help = "Launch configured agent_cli in the designated pane")]
    pub run_agent: bool,
}
```

- [ ] **Step 2: `handle_start` 把 `args` 透传给 `launch_zellij`**

把 `launch_zellij` 的签名改为：

```rust
fn launch_zellij(
    config_mgr: &ConfigManager,
    global: &crate::config::global::GlobalConfig,
    workspace: &WorkspaceConfig,
    runner: &RealRunner,
    run_agent: bool,
) -> Result<()> {
```

并在 `handle_start` 末尾的调用处改为：

```rust
    if !args.no_zellij {
        launch_zellij(&config_mgr, &global, &workspace, &runner, args.run_agent)?;
    }
```

`handle_open` 中调用 `launch_zellij` 的那一行也要改（`zootree open` 本次不支持 `--run-agent`，永远传 `false`）：

```rust
    launch_zellij(&config_mgr, &global, &workspace, &runner, false)?;
```

- [ ] **Step 3: 在 `launch_zellij` 中按规则表构造 `(overview_kdl, repo_kdl_for_first)`**

在 `launch_zellij` 函数体内，在 `let ws_dir = ...` 之后、构造 `LayoutVar` vec 之前，插入：

```rust
    let (overview_kdl, repo_kdl_for_first) = if run_agent {
        let agent_cli_tpl = global.agent_cli.as_deref().ok_or_else(|| {
            anyhow::anyhow!("--run-agent requires agent_cli in global config (~/.config/zootree/config.toml)")
        })?;
        let prompt = crate::core::layout::build_prompt(workspace);
        let kdl = crate::core::layout::build_agent_cli_kdl(agent_cli_tpl, &prompt)?;
        if workspace.repos.len() == 1 {
            (String::new(), kdl)
        } else {
            (kdl, String::new())
        }
    } else {
        (String::new(), String::new())
    };
```

- [ ] **Step 4: 把 `LayoutVar` 构造改为分发规则表**

把 `for repo_entry in &workspace.repos { ... vars.push(LayoutVar { ... }) }` 循环改为：

```rust
    let mut vars = Vec::new();
    for (i, repo_entry) in workspace.repos.iter().enumerate() {
        let repo_config = config_mgr.load_repo_config(&repo_entry.name)?;
        let lazygit_config = repo_config.lazygit.map(|lg| lg.config).unwrap_or_default();

        let repo_agent_cli = if i == 0 {
            repo_kdl_for_first.clone()
        } else {
            String::new()
        };

        vars.push(LayoutVar {
            repo_name: repo_entry.name.clone(),
            worktree_path: format!("{}/{}", ws_dir, repo_entry.name),
            branch: workspace.branch.clone(),
            workspace_name: workspace.name.clone(),
            workspace_dir: ws_dir.clone(),
            lazygit_config,
            overview_agent_cli: overview_kdl.clone(),
            repo_agent_cli,
        });
    }
```

注意：所有 `LayoutVar` 实例的 `overview_agent_cli` 都设为同一值 `overview_kdl.clone()`（`LayoutRenderer::render` 在 overview 段只用 `repos.first()` 的 vars，但全部设一致更稳定）。

- [ ] **Step 5: 在渲染完模板后做占位符缺失检查（warn 不 bail）**

在 `let rendered = LayoutRenderer::render(&template_content, &vars);` 之后立即追加：

```rust
    if run_agent
        && !template_content.contains("$overview_agent_cli")
        && !template_content.contains("$repo_agent_cli")
    {
        tracing::warn!(
            "--run-agent is set but layout '{}' contains no $overview_agent_cli or $repo_agent_cli placeholder; agent_cli will not be executed",
            layout_name
        );
    }
```

- [ ] **Step 6: 编译检查**

Run: `cargo check --tests`
Expected: 编译通过。

- [ ] **Step 7: 运行已有测试确认无回归**

Run: `cargo test`
Expected: 所有已有测试通过。

- [ ] **Step 8: Commit**

```bash
git add src/cli/workspace.rs
git commit -m "feat(start): add --run-agent flag and dispatch agent_cli into layout panes"
```

---

## Task 7: 端到端测试 `tests/start_agent_test.rs`

**Files:**
- Test: `tests/start_agent_test.rs`（新建）

集成测试不真跑 zellij、git，直接调用 `LayoutRenderer::render(default_layout(), vars)` + `build_agent_cli_kdl` + `build_prompt`，验证规则表四个分支的渲染结果。

- [ ] **Step 1: 新建 `tests/start_agent_test.rs`**

```rust
use zootree::config::global::ZellijConfig;
use zootree::config::workspace::{RepoEntry, WorkspaceConfig};
use zootree::core::layout::{
    build_agent_cli_kdl, build_prompt, LayoutRenderer, LayoutVar,
};

fn make_workspace(repos: Vec<&str>) -> WorkspaceConfig {
    WorkspaceConfig {
        title: "Add login flow".into(),
        name: "calm-river".into(),
        description: "Implement OAuth2".into(),
        branch: "zootree/calm-river".into(),
        workspace_dir: "/ws/calm-river".into(),
        created_at: "2026-05-12T00:00:00+08:00".into(),
        zellij: ZellijConfig::default(),
        repos: repos
            .into_iter()
            .map(|n| RepoEntry {
                name: n.into(),
                target_branch: Some("main".into()),
            })
            .collect(),
        events: Vec::new(),
    }
}

/// Mirrors the rule table in `launch_zellij`. Pure function over inputs so the
/// test doesn't need to spawn zellij or hit the filesystem.
fn render_with_rule(
    workspace: &WorkspaceConfig,
    run_agent: bool,
    agent_cli: Option<&str>,
) -> anyhow::Result<String> {
    let (overview_kdl, repo_kdl_for_first) = if run_agent {
        let tpl = agent_cli.ok_or_else(|| {
            anyhow::anyhow!("--run-agent requires agent_cli in global config")
        })?;
        let prompt = build_prompt(workspace);
        let kdl = build_agent_cli_kdl(tpl, &prompt)?;
        if workspace.repos.len() == 1 {
            (String::new(), kdl)
        } else {
            (kdl, String::new())
        }
    } else {
        (String::new(), String::new())
    };

    let mut vars = Vec::new();
    for (i, repo) in workspace.repos.iter().enumerate() {
        vars.push(LayoutVar {
            repo_name: repo.name.clone(),
            worktree_path: format!("{}/{}", workspace.workspace_dir, repo.name),
            branch: workspace.branch.clone(),
            workspace_name: workspace.name.clone(),
            workspace_dir: workspace.workspace_dir.clone(),
            lazygit_config: String::new(),
            overview_agent_cli: overview_kdl.clone(),
            repo_agent_cli: if i == 0 {
                repo_kdl_for_first.clone()
            } else {
                String::new()
            },
        });
    }

    Ok(LayoutRenderer::render(LayoutRenderer::default_layout(), &vars))
}

fn split_overview_and_repo_tabs(rendered: &str) -> (String, String) {
    // overview tab is the first `tab name="overview" { ... }` block; repo tabs
    // come after. We split on the second `tab name=` occurrence.
    let first_tab_start = rendered.find(r#"tab name="overview""#).expect("overview tab missing");
    let after_first = &rendered[first_tab_start..];
    let second_tab_rel = after_first[1..]
        .find(r#"tab name=""#)
        .expect("repo tab missing");
    let split_at = first_tab_start + 1 + second_tab_rel;
    (rendered[..split_at].to_string(), rendered[split_at..].to_string())
}

#[test]
fn run_agent_with_one_repo_injects_into_repo_pane_only() {
    let ws = make_workspace(vec!["frontend"]);
    let rendered = render_with_rule(&ws, true, Some("claude --dangerously-skip-permissions -- $prompt"))
        .unwrap();

    let (overview, repo_section) = split_overview_and_repo_tabs(&rendered);
    assert!(
        !overview.contains(r#"command="claude""#),
        "overview should NOT contain agent: {}",
        overview
    );
    assert!(
        repo_section.contains(r#"command="claude""#),
        "repo section should contain agent: {}",
        repo_section
    );
    assert!(
        repo_section.contains(r#""--dangerously-skip-permissions""#),
        "agent args present in repo section: {}",
        repo_section
    );
    assert!(
        repo_section.contains(r#""Add login flow\nImplement OAuth2""#),
        "prompt joined with newline (escaped): {}",
        repo_section
    );
}

#[test]
fn run_agent_with_two_repos_injects_into_overview_only() {
    let ws = make_workspace(vec!["frontend", "backend"]);
    let rendered =
        render_with_rule(&ws, true, Some("claude -- $prompt")).unwrap();

    let (overview, repo_section) = split_overview_and_repo_tabs(&rendered);
    assert!(
        overview.contains(r#"command="claude""#),
        "overview contains agent: {}",
        overview
    );
    assert!(
        !repo_section.contains(r#"command="claude""#),
        "no repo tab contains agent: {}",
        repo_section
    );
}

#[test]
fn no_run_agent_keeps_layout_clean() {
    let ws = make_workspace(vec!["frontend"]);
    let rendered =
        render_with_rule(&ws, false, Some("claude -- $prompt")).unwrap();

    assert!(!rendered.contains(r#"command="claude""#));
    assert!(!rendered.contains("$overview_agent_cli"));
    assert!(!rendered.contains("$repo_agent_cli"));
}

#[test]
fn run_agent_without_agent_cli_errors() {
    let ws = make_workspace(vec!["frontend"]);
    let err = render_with_rule(&ws, true, None).unwrap_err();
    let msg = format!("{:#}", err);
    assert!(
        msg.contains("agent_cli"),
        "error mentions agent_cli: {}",
        msg
    );
}

#[test]
fn agent_cli_with_embedded_prompt_token() {
    let ws = make_workspace(vec!["frontend"]);
    let rendered =
        render_with_rule(&ws, true, Some("claude --prompt=$prompt")).unwrap();

    let (_, repo_section) = split_overview_and_repo_tabs(&rendered);
    assert!(
        repo_section.contains(r#""--prompt=Add login flow\nImplement OAuth2""#),
        "embedded prompt substituted: {}",
        repo_section
    );
}
```

注意：测试用 `split_overview_and_repo_tabs` 在第二个 `tab name="` 处切分（即 overview tab 结尾、repo tab 起点），避免在不可靠的 brace counting 上花时间。

- [ ] **Step 2: 运行测试**

Run: `cargo test --test start_agent_test`
Expected: 5 个测试全部通过。

- [ ] **Step 3: 运行完整测试套件无回归**

Run: `cargo test`
Expected: 全部通过。

- [ ] **Step 4: Commit**

```bash
git add tests/start_agent_test.rs
git commit -m "test(start): add agent_cli rule-table integration tests"
```

---

## Task 8: 更新 zootree-dev skill 文档

**Files:**
- Modify: `.claude/skills/zootree-dev/SKILL.md`

skill 自我迭代规则要求新增依赖时同步更新文档。

- [ ] **Step 1: 在「关键依赖」表格末尾追加 `shlex` 行**

定位 `| Crate | 用途 |` 表格，在 `chrono` 行之后、`cargo-husky` 行之前插入：

```markdown
| `shlex` (1) | 把 `agent_cli` 字符串模板拆成 argv |
```

- [ ] **Step 2: 验证 skill 与代码同步**

Run: `grep -A 100 '\[dependencies' Cargo.toml | grep -v '^\[' | grep -v '^$' | grep -v '^#'`
Expected: 输出包含 `shlex`，与 SKILL.md 表格一致。

- [ ] **Step 3: Commit**

```bash
git add .claude/skills/zootree-dev/SKILL.md
git commit -m "docs(skill): add shlex to zootree-dev dependency list"
```

---

## Task 9: 全量验证

**Files:** 无

- [ ] **Step 1: 全量构建**

Run: `cargo build`
Expected: 无 warning 阻塞、无错误。

- [ ] **Step 2: 全量测试**

Run: `cargo test`
Expected: 全部通过。

- [ ] **Step 3: clippy 检查**

Run: `cargo clippy --all-targets -- -D warnings`
Expected: 无新增 warning。

- [ ] **Step 4: 手动冒烟（可选，用户授权后再做）**

```bash
# 在测试 workspace 中
cargo run -- start <some-pending-workspace> --run-agent
# 观察 ~/.config/zootree/layouts/recently.kdl，确认 overview 或 repo pane 含 command="..."
```

未配置 `agent_cli` 时：

```bash
cargo run -- start <some-pending-workspace> --run-agent
# 期望立即报错："--run-agent requires agent_cli in global config (...)"
```

---

## Self-Review Notes

- ✅ Spec 全部决策点都有对应任务（agent_cli 字段 → T2；shlex/$prompt 解析 → T4；prompt 拼接 → T3；layout vars → T5；规则表 → T6；占位符缺失 warn → T6 step 5；老 layout 兼容 → T5 中 default_layout 的占位符前空格策略；端到端 → T7）。
- ✅ 所有占位符（"TODO", "fill in"）已消除——每个 step 都给出完整代码或精确命令。
- ✅ 类型与方法签名前后一致：`build_prompt(&WorkspaceConfig) -> String`、`build_agent_cli_kdl(&str, &str) -> Result<String>`、`LayoutVar` 字段名 `overview_agent_cli` / `repo_agent_cli` 在 T5 / T6 / T7 中保持一致。
- ✅ Spec "待 writing-plans 阶段细化" 三项已落实：
  - shlex 版本号锁定为 `1.3`（与 Cargo.lock 一致，避免触发新版本拉取）
  - 错误文案：`--run-agent requires agent_cli in global config (~/.config/zootree/config.toml)` / `agent_cli is empty` / `failed to parse agent_cli: <tpl>`
  - default_layout 占位符前一个空格、替换为空串后 KDL 仍合法

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-05-12-agent-cli.md`. Two execution options:

**1. Subagent-Driven (recommended)** — 每个 task 派一个全新 subagent，期间双段评审，迭代快、上下文干净。

**2. Inline Execution** — 在当前会话用 executing-plans 跑批，按 checkpoint 暂停评审。

Which approach?
